/**
 * @namespace {Taro Scaff}
 * ======== ======== ======== ======== ========
 * @contributor:
 * @module {sugar}      - joenix
 * @module {dependence} - joenix
 * ======== ======== ======== ======== ========
 * @runtime:
 * @client   {h5}         - h5
 * @applet   {weapp}      - wechat
 * @applet   {alipay}     - alipay
 * @applet   {qq}         - qq
 * @applet   {swan}       - baidu
 * @applet   {tt}         - bytedance
 * @quickapp {quickapp}   - quickapp
 * @native   {rn}         - react native
 * ======== ======== ======== ======== ========
 * @support:
 *
 * @language {react} - react
 *
 * @cli {cli} - taro-cli
 * @ui  {ui}  - taro-ui
 *
 * @promise {async/await} - promise
 * @route   {router}      - router
 *
 * @component {components}    - h5
 * @component {components-rn} - react native
 * @component {components-qa} - quickapp
 *
 * @store {redux}    - applet
 * @store {redux-h5} - h5
 * @store {redux-rn} - rn
 *
 * @convenience {redux-logger} - logger
 * @convenience {redux-thunk}  - thunk
 *
 * @compile {jsx}     - transformer-wx
 * @compile {applet}  - taroize
 * @compile {webpack} - webpack-runner
 * @compile {rn}      - taro-rn-runner
 * ======== ======== ======== ======== ========
 */

// Dependencies
import * as Dependencies from "./dependencies";

// Export
export default Dependencies;
